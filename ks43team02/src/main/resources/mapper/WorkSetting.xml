<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ks43team02.mapper.WorkSettingMapper">
	<resultMap type="WorkSetting" id="WorkSettingMap">
		<result column="standard_work_code" 		property="standardWorkCode"/>
		<result column="worksystem_name" 			property="worksystemName"/>
		<result column="reg_user"					property="regUser"/>
		<result column="reg_date" 					property="regDate"/>
	</resultMap>
	<select id="getWorkSettingName" parameterType="WorkSetting" resultMap="WorkSettingMap">
		/* 근무제 가져오기 */
		SELECT
			ws.standard_work_code
			,ws.worksystem_name
			,ws.reg_user
			,ws.reg_date
		FROM
		tb_worksystem_standard AS ws;
	</select>
	<insert id="addWorkSysName" parameterType="workSetting">
		/*근무제리스트 추가*/
		INSERT INTO tb_worksystem_standard
		(
		worksystem_name
		, reg_date
		)
		VALUES 
		(
		#{worksystemName}
		, CURDATE()
		);
	</insert>
</mapper>